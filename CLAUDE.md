# CLAUDE

* Don't write tests yet
* All text in the app should be in French, use the `fr_FR` locale but the code should be in english (like variable names, function names...)
* Use shadcn
* The project is hosted on CloudFlare. use only features available on CloudFlare Workers in the free plan.
